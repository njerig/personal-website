# meta

title: "njerig.me"
display-title: "njerig"
description:
url: "."


# build

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable: true
plugins: [jekyll-sitemap, jekyll-admin]
exclude: [Gemfile, Gemfile.lock, vendor]


# site vars

permalink: /blog/:title